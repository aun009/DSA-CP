#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    if (cin >> n) {
        cout << "Output: " << n << endl;
    } else {
        // This will help you debug if the input file is empty
        cout << "No input detected!" << endl;
    }

    return 0;
}