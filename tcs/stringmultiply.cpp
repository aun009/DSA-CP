#include <iostream>
using namespace std;

void solve() {
    
    string str;
    cin >>str;

    stringstream ss(str);

    while(ss > str) {
    	
    }
    
}

int main() {
    // Fast I/O
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while (t--) {
        solve();
    }

    return 0;
}