#include <iostream>
using namespace std;
void solve() {
    
    int n;
    cin >> n;

    // string s = 55;

    cout << n << endl;
    // if(n == 1) {
    // 	cout << "Prime" << endl;
    // 	return;
    // }
    // int cnt = 0;
    // for(int i=2; i<n*n; i++) {
    // 	if(n % i == 0) cnt = 1;  // Changed != to ==
    // }
    // if(cnt == 1) cout << "NO" << endl;
    // else cout << "YES" << endl;
    
}
int main() {
    // Fast I/O
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}